# PHP Docker

```bash
# Run PHP server
$ make up

# Access PHP shell
$ make php
```

# Install Composer.

```bash
$ curl -sS https://getcomposer.org/installer | php

$ php composer.phar require guzzlehttp/guzzle:^7.0
```
